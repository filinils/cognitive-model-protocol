# LLM Usage

The canonical protocol definition is [reference/current.md](https://github.com/filinils/cognitive-model-protocol/blob/main/reference/current.md).

LLMs should read that document first.

When using CMP, prefer identifying uncertainty and possible falsifiers over strengthening a user's existing self-model. CMP models collaboration with a user, not the user's identity.
